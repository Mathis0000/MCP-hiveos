services:
  hiveos-mcp:
    build: .
    container_name: hiveos-mcp
    env_file: .env
    restart: unless-stopped
    volumes:
      - ./ssh:/app/ssh:ro
    networks:
      - default
      - caddy_net

networks:
  caddy_net:
    external: true
    name: maraicher_les_graviers_maraicher_network
